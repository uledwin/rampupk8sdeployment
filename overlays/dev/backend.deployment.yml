apiVersion: apps/v1
kind: Deployment
metadata:
  name:  backend
  labels:
    app:  backend
    component: backend
    environment: development 
spec:
  template:
    spec:
      containers:
      - name: backend
        image: uledwin/backend:1.2.9
